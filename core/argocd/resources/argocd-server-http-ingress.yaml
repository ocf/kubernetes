apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-http-ingress
  namespace: argocd
spec:
  rules:
  - host: argo.ocf.berkeley.edu
    http:
      paths:
      - backend:
          service:
            name: argocd-server
            port:
              number: 80