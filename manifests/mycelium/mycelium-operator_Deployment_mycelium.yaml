apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mycelium-operator
  name: mycelium-operator
  namespace: mycelium
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mycelium-operator
  template:
    metadata:
      annotations:
        prometheus.io/port: '8080'
        prometheus.io/scrape: 'true'
      labels:
        app: mycelium-operator
    spec:
      containers:
      - env:
        - name: MYCELIUM_FW_TOKEN
          valueFrom:
            secretKeyRef:
              key: forwarding_token
              name: mycelium-operator
        - name: MYCELIUM_ENDPOINT
          value: mycelium-operator.mycelium.svc.cluster.local:8080
        - name: MYCELIUM_RUNNER_IMAGE
          value: harbor.ocf.berkeley.edu/mycelium/runner:0.3.0
        - name: MYCELIUM_PLUGIN_VELOCITY
          value: https://www.ocf.berkeley.edu/~njha/artifacts/mycelium-velocity-0.3.0.jar
        - name: MYCELIUM_PLUGIN_PAPER
          value: https://www.ocf.berkeley.edu/~njha/artifacts/mycelium-paper-0.3.0.jar
        image: harbor.ocf.berkeley.edu/mycelium/operator:0.3.0
        imagePullPolicy: IfNotPresent
        name: operator
        ports:
        - containerPort: 8080
          name: api
        readinessProbe:
          httpGet:
            path: /health
            port: api
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          limits:
            cpu: 200m
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 100Mi
      serviceAccountName: mycelium-operator
